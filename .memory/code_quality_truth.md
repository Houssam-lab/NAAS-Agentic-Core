# حقيقة أدوات جودة الكود — ماذا وجدت كلٌّ منها، وكم من ذلك صحيح

> **الحالة، لا القانون.** القرار الهندسي في `.memory/decisions.md` D-235.
> كل رقمٍ هنا **مؤرَّخ بمصدره** — رقمٌ بلا تشغيلٍ يُنسَب إليه يتقادم ثمّ يكذب (D-188).
> آخر تحقّق: **2026-08-09** · Qodana على `238633a` (تكرّر على `3b975d8` و`62ce4c2`) ·
> CodeScene على `a814655` و`62ce4c2` · CodeRabbit على PR #5 و#6.
>
> **تحقّقٌ لاحق: 2026-08-10 على PR #7** (D-236 · ISS-152) — القسم 0 أدناه.

## 0) تشغيل PR #7 (2026-08-10) — الأدوات الثلاث على كودٍ جديد

**القيمة المُثبَتة: الأدوات وجدت ما لم تجده الاختبارات ولا بوّابات `scripts/fitness/`.**

> **كل رقمٍ هنا مربوطٌ بالـcommit الذي أنتجه** (D-188، ورصدت المراجعة أنّ النسخة
> الأولى من هذا القسم تركت أرقام Qodana بلا مرجع). الأساس المُقارَن به في كل
> صفوف Qodana هو `543163a` (قاعدة الـPR)، والعتبة `--fail-threshold 2405` في
> `.github/workflows/qodana_code_quality.yml`.

| الأداة | الـcommit | ما وجدته | الحكم | ما فُعل |
|--------|-----------|----------|-------|---------|
| **CodeScene** | `6283d5b` | **5 ملفّات** بـ12 خرقاً (Complex Method · Bumpy Road · Excess Arguments · Complex Conditional · Overall Complexity) | ✅ **كلّها حقيقية** | إعادة بناء لا كتم |
| **CodeScene** | `677e2a3` | بقي ملفٌّ واحد (البوّابة نفسها) بـ4 خروق | ✅ حقيقي | أُصلح |
| **CodeScene** | `7ae13e7` | **6/6 بوّابات نجحت** — موافقة · `auth_boundary_service.py` **9.06 → 9.47** | — | — |
| **CodeRabbit** | `677e2a3` | (1) بحثٌ في سلسلة النماذج الأولية: `error_code:"constructor"` يُرجع **دالّة** تصل طبقة العرض · (2) ثلاث نسخ من جداول الترجمة **بلا بوّابة تقارنها** | ✅ **كلاهما حقيقي** | `hasOwnProperty.call` في النسخ الثلاث + اختبار **أحمر على الكود القديم** (٨ إخفاقات تُظهر `function Object() { [native code] }`) · والبوّابة تقارن الجداول الآن |
| **CodeRabbit** | `4cdcd0b` | **4 ملاحظات** أبرزها: `_JS_ENTRY` تقبل الاقتباس المفرد وحده ⇒ **سقوطٌ صامت** يجعل البوّابة تطبع النجاح على نصٍّ لم تقرأه · وفحوص المرآة في اختبار الواجهة **لا تربط** الحارس بموضع البحث | ✅ **الأربع حقيقية** | الأربع أُصلحت في هذا الـcommit؛ التفصيل في رسالة الـcommit |
| **Qodana** | `677e2a3` | **2404** (من 2405) · لكنّ `Hardcoded passwords` **2 → 5** | ⚠️ الثلاثة الجدد **كانت لي** | ظننتُها من إسنادات كلمات السرّ فأصلحتُ الموضع الخطأ |
| **Qodana** | `7ae13e7` | **2407** — ❌ **فوق العتبة، فأحمر** (`Redundant escape` 95→96 · `Duplicated fragment` 235→236 · `Regex can be simplified` 0→1 · وHardcoded ما زالت 5) | ⚠️ الثلاثة الجدد **كانت لي أيضاً** | `4cdcd0b`: `[\w]`→`\w` · `\{`→`{` · مساعد `_login()` وثابت `_WRONG` يزيلان التكرار والحرفيات معاً |

**⛔ لا كتم في أيٍّ من هذه الصفوف.** `qodana.yaml` **لم يُمَسّ في هذا الـPR** —
يمكن التحقّق بـ`git diff 543163a..HEAD -- qodana.yaml` (فارغ). والثمانية الكاذبة
الموثَّقة في §2 بقيت مُعدَّدة كما هي (قاعدة D-235)، والكتم كان سيُنقِص العدد بلا
أثرٍ على أيّ بوّابة لأنّ Qodana **ليست ضمن `required-ci`** (`ci.yml` — قائمة
`needs` لا تذكرها).

**درسٌ مقيس:** ثلاث ملاحظات من أصل أربع كانت على **الكود الذي كتبته في هذا الـPR**،
لا على الدَّين القديم. والملاحظة الأشدّ إيلاماً أنّ **البوّابة نفسها** كانت أعقد ملفّ
في الديفّ — أي أنّ الحارس الذي يفرض البساطة كان أسوأ مخالفٍ لها. وCodeRabbit لاحظ أنّ
حجّة الـPR («قاعدةٌ بلا فارضٍ آلي ليست قاعدة») تنطبق على قواعد الـPR نفسه: جداول
الترجمة الثلاث كانت محروسةً **بتعليق**.

⛔ **ولم يُكتَم شيء.** الثمانية الكاذبة الموثَّقة في §2 بقيت كما هي (قاعدة D-235)،
و`qodana.yaml` لم يُمَسّ — الكتم كان سيُنقِص العدد بلا أثرٍ على أي بوّابة، لأن
العتبة `2405` وQodana **ليست ضمن `required-ci`** أصلاً.

## 1) الأدوات الثلاث تقيس ثلاثة أشياء مختلفة — والخلط بينها أصل التوقّع الخاطئ

| | Qodana | CodeScene | CodeRabbit |
|---|--------|-----------|------------|
| **ماذا يفعل** | تحليل ساكن يبحث عن **أخطاء** في الكود | يقيس **قابلية الصيانة** (Code Health) ويستقبل **التغطية** | يقرأ **النيّة**: يقارن ما يقوله الـPR بما يفعله الديفّ |
| **متى يحكم** | على المستودع كلّه في كل تشغيل | على **الكود المتغيّر في الـPR** (delta) | على الديفّ + كل commit لاحق |
| **مخرَجه** | قائمة نتائج بدرجات (Critical/High/Moderate) | بوّابات جودة تنجح أو تفشل + لوحة تغطية | تعليقات مراجعة بأدلّة (سكربت مُشغَّل · بحث ويب) |
| **أين يُقرأ** | `qodana.cloud/projects/b890K` | `codescene.io/projects/83416` | تعليقات الـPR نفسها |
| **حارسه في المستودع** | `--fail-threshold` داخل `args` في `.github/workflows/qodana_code_quality.yml` | تطبيق GitHub (خارج المستودع) + وظيفة `codescene-coverage` في `ci.yml` | `.coderabbit.yaml` — والبوّابة `check_ci_workflow_hygiene` تفرض أن يقرأ الدستور ولا يحجب الدمج |

⛔ **لا يُتوقَّع أن تجد الثلاث الشيء نفسه.** Qodana لم يقل شيئاً عن تعقيد الدوالّ،
وCodeScene لم يقل شيئاً عن `dict[str, any]`، ولم يلحظ أيٌّ منهما أنّ راية البوّابة نفسها
مُسقَطة بصمت — وهو ما رآه CodeRabbit وحده. كلٌّ يرى ما صُمِّم ليراه.

## 2) ما وجده Qodana — **2,405 نتيجة على `1b8cd82`، مُعدَّدة بالكامل**

**⚠️ العدد ليس درجة جودة.** فُحصت النتائج **الحرجة الثلاث عشرة يدوياً** (قبل الإصلاح على
`238633a`)، فكانت **5 حقيقية و8 كاذبة** — أي أنّ «Failure» في Qodana **ليس مرادفاً
لـ«خطأ حقيقي»**. الخمس أُصلحت، فبقيت **الثماني الكاذبة وحدها** في المستوى الأعلى.

**الجداول أدناه كاملة لا جزئية**: ٣٥ فئة تجمع **8 + 319 + 2,078 = 2,405** بالضبط
(مُتحقَّقٌ حسابياً من تقرير التشغيل). لا تبقى فئةٌ غير مذكورة، فلا يُقرأ الفراغ صفراً.

### 🔴 Failure (8) — **كلّها كاذبة**، وحكمٌ لكلٍّ

| العدد | الموضع | النتيجة | الحكم |
|---|--------|---------|-------|
| 2 | `scripts/set_admin_password.py:2` · `live_db_restructure.py:2` | «Hardcoded password» | ❌ كاذب — docstring فيه `postgres://user:pass@host` كمثال استعمال |
| 1 | `observability/grafana/dashboards/90-…-planning-agent.json:330` | «curl-auth-header» | ❌ كاذب — مثال curl داخل لوحة نصّية |
| 2 | `.ona/automations.yaml:766` · `:1089` | «curl-auth-header» · «Password in URL» | ❌ كاذب — سكربتا تحقّق |
| 3 | `app/middleware/config/middleware_settings.py` | «Invalid type hints» على `ConfigValue` | ❌ كاذب — `ConfigValue` اسم نوعٍ ضمني صحيح؛ Qodana لا يتعرّف عليه بلا `: TypeAlias` |

**دليلٌ مستقلّ على أنّ «Hardcoded passwords» كاذبة:** بوّابة `check_no_committed_secrets`
تمرّ بدَينٍ مُجمَّد **فارغ**، وهي تبحث عن أنماط الأسرار **الحقيقية** (`sk-or-v1-` ·
`tvly-` · JWT · كلمة سرّ فعلية في رابط قاعدة بيانات).

**المُصلَحتان اختفتا من هذا المستوى:** الخمسُ `dict[str, any]` (كانت Failure) والواحدة
«جملة بلا أثر» (كانت Warning) — والعدّ نزل **2,411 → 2,405** بالضبط، أي ستّة. برهانٌ
مستقلّ على أنّ الإصلاحين وصلا.

### 🔶 Warning (319) — **الستّ عشرة فئة كاملةً**

| العدد | الفئة | الحكم |
|------|-------|-------|
| 106 | `Deprecated function, class, or module` | لم تُفحَص — تنظيف مُجدٍ |
| 95 | `Redundant character escape` | لم تُفحَص — تنظيف آلي |
| 44 | `Unused imports` | لم تُفحَص — تنظيف آلي |
| **28** | `Attempt to call a non-callable object` | ⚠️ **لم تُفحَص فرداً فرداً** — بعضها ديكوريترات (كاذبة على الأرجح)، وبعضها في مسار الطالب: `probability_ui.py:156` · `planning_agent/graph.py:64` |
| 7 | `The default argument is mutable` | ❌ كاذب — نمط تجميد الإغلاق المقصود (`customer_chat.py:462`) |
| 7 | `Invalid protocol definitions and usages` | لم تُفحَص |
| 7 | `Invalid TypedDict definition and usages` | لم تُفحَص |
| 6 | `Unbound local variables` | ❌ كاذب في `app/services/chat/local_graph.py:889,900` — الحارس `if root_span_ctx:` يضمن ربط `obs` (س827–834)، والمحلّل لا يربط بين المتغيّرَين |
| 6 | `Unreachable code` | ⚠️ **لم تُفحَص** |
| 4 | `Invalid type hints definitions and usages` | لم تُفحَص (بقيّة الصنف الذي كان 2,607 قبل كشف الكاش المسموم) |
| 3 | `Unnecessary non-capturing group` | تنظيف آلي |
| 2 | `Consecutive spaces` | تنظيف آلي |
| 1 | `Method signature does not match overridden` | لم تُفحَص |
| 1 | `Stub packages advertiser` | اقتراح تبعية، لا عطب |
| 1 | `Single character alternation` | تنظيف آلي |
| 1 | `Begin or end anchor in unexpected position` | لم تُفحَص |

### ◽️ Notice (2,078) — أسلوبية، **السبع عشرة فئة كاملةً**

`Method is not declared static` 658 · `Unclear exception clauses` 315 ·
`Unused local symbols` 265 · `Duplicated code fragment` 237 ·
`String conversion without dunder method` 236 · `Accessing a protected member` 175 ·
`Shadowing names from outer scopes` 107 · `Redundant parentheses` 23 ·
`Shadowing built-in names` 20 · `Function calls that don't return anything as values` 17 ·
`Non-optimal list declaration` 9 · `Unnecessary type cast` 5 ·
`Invalid abstract class definition` 4 · `Redundant boolean variable check` 4 ·
`Too complex chained comparisons` 1 · `Dictionary creation … dictionary literal` 1 ·
`Incorrect docstring` 1.

## 3) ما وجده CodeScene

**صحّة الكود (delta):** على `457205b` **أفشل الـPR** ببندين في ملفّ البوّابة الجديد:

```text
_check_token_guard         cyclomatic complexity 9, threshold 9
_check_third_party_write   5 arguments, max 4
```

أُصلحا بإعادة بناء (تعقيد **7** · وسائط **3**)، فتحوّل الفحص إلى **موافقة**:
«6 Quality Gates Passed» على `a814655`. ⚠️ ثلاثٌ من الستّ لم تُسمَّ في المخرَج، فلا
يُدَّعى معرفتها.

**ثمّ أفشل الملفّ نفسه مرّةً ثانية** على `23e0800` حين كبر ببند CodeRabbit:

```text
_guideline_patterns    Complex Method      cyclomatic complexity 9, threshold 9
_check_coderabbit_law  Bumpy Road Ahead    2 blocks with nested conditional logic
```

فُكِّكت الأولى إلى `_pattern_entry` + `_guideline_patterns` (تعقيد **5** و**6**)،
والثانية إلى `_check_required_guidelines` + `_check_guideline_targets` (**2** و**4**)،
وأُعيد تشغيل التجارب السلبية الستّ **كلّها بعد التفكيك** فبقيت تُطلق. ⛔ **ولم يُستعمَل
زرّ `Suppress`** الذي يعرضه CodeScene في كل بلاغ: إسكاتُ قياسٍ صحيح يخدم الرقم لا القارئ
(نفس قاعدة الإنذارات الكاذبة، بالاتجاه المعاكس).

**وهذه أنفع مساهمةٍ مقيسة لـCodeScene:** مرّتين على ملفٍّ واحد، وفي كلتيهما كان محقّاً،
وفي كلتيهما كان الكاتب **وكيلاً** لا إنساناً متعباً. أداةٌ تقيس تعقيد ما يُكتَب الآن تعوّض
غيابَ الحدس الذي يوقف الإنسان.

**التغطية:** `codescene-coverage` ترفع **691 ملفّاً** من `main` (`62ce4c2`)، وباسم الفرع
الصحيح بعد إصلاح HEAD المنفصل. ⚠️ لم تُرفَع بايتاً واحداً قبل 2026-08-09.

## 3.4) ما وجده CodeRabbit — **خمسة، كلّها حقيقية** (PR #5 · #6)

الأداة الوحيدة التي **تقرأ النيّة**: تقارن ما يقوله وصف الـPR بما يفعله الديفّ فعلاً،
وتُشغّل سكربتات وتبحث في الويب لتتحقّق. ولذلك رأت صنفاً لا تراه أداةٌ ساكنة — **العطب
في الغياب** لا في الكود الحاضر.

| # | ما وجده | لماذا هو حقيقي |
|---|---------|---------------|
| 1 | **حقن سكربت** — `${{ github.head_ref }}` مُدرَجاً داخل كتلة `run:` في خطوةٍ تحمل `CS_ACCESS_TOKEN` | اسمُ الفرع يتحكّم به مُقدِّم الـPR؛ الاستيفاء يجري **قبل** الصدفة، فاسمٌ ملغوم ينفّذ أوامر في وظيفةٍ تملك السرّ. أُصلح بنقله إلى `env:` (نفس منطق D-187: الحقن يصير **غير مُمثَّل**) |
| 2 | **المِسنَن بلا أسنان** — `fail-threshold:` مفتاحاً مستقلاً تحت `with:` | راجع `action.yaml` الخاصّ بـ`qodana-action@v2026.1`: لا مُدخَل بهذا الاسم، وGitHub يُسقِط غير المُعلَن **بصمت**. الفحص أخضر والبوّابة غير موجودة — commitين |
| 3 | **ادّعاء غيابٍ لم يُفحَص** — «لا إصدار أحدث من `download-artifact@v6`» | شغّل `rg` على الشجرة وبحث في الويب: v7 (ديسمبر 2025) وv8 (فبراير 2026) موجودان وكلاهما node24 |
| 4 | **جدولٌ جزئي يُقرأ كاملاً** — صفوف الشدّة تجمع 293 من 320 و1,886 من 2,078 | قارئٌ يجمع الأرقام يظنّ أنّ ما بقي صفر. وُسمت الجداول **جزئية** صراحةً (L11) |
| 5 | **MD040** — سياجُ كودٍ بلا وسم لغة | أسلوبي، وأُصلح بـ`text` |

**صفر إنذارٍ كاذب على هذين الـPR** — عيّنة صغيرة (٢ PR)، فتُقرأ كسجلٍّ لا كمعدّل.

**الحدّ المقيس، ومعالجته:** CodeRabbit **وافق** على `dict[str, Any]` (الجدول أدناه) —
لأنه كان يعمل بالافتراضات: يعرف **اللغة** ويجهل **قانون هذا المستودع**. عولج بملفّ
`.coderabbit.yaml` يوجّه `knowledge_base.code_guidelines` إلى `CLAUDE.md` وعقائد
`docs/architecture/`، و`path_instructions` تنطق القوانين المخروقة فعلاً (حظر `Any` ·
`os.environ` · SQLAlchemy المتزامن · `shell=True` · الاستيفاء في `run:` · فحص الراية
من `action.yaml`). ⛔ وهو **مُقيَّد بألّا يحجب الدمج** (`fail_commit_status: false`،
ولا `pre_merge_checks`) — نفس قانون D-234 الذي يُبقي Qodana وCodeScene خارج
`required-ci`. تفرض الأمرين `check_ci_workflow_hygiene` بستّ تجارب سلبية مُثبَتة.

⚠️ **ومعنى `path_filters` غير موثَّق للحالة التي نستعملها**: المرجع المنشور يقول «include
or exclude» ويُمثِّل بـ`!dist/**`، ولا يقول ماذا تعني قائمةٌ **كلّها استثناءات** — أهي
«كلُّ شيءٍ ناقص هذه» أم «لا شيء لأن لا نمط تضمينٍ طابق»؟ وتحت القراءة الثانية تُراجَع
**صفرُ ملفّات** بأخضرَ كامل — نفس الفشل الصامت الذي أُزيل هنا طوال اليوم. فأُضيف تضمينٌ
صريح `"**"` في الصدارة: **لا-عمليةٌ تحت القراءة الأولى، وإصلاحٌ تحت الثانية**. ⛔ لا
يُحذَف «تبسيطاً» قبل جوابٍ موثَّق. **الغموض يُعالَج باختيارٍ سليمٍ تحت كل القراءات، لا
بترجيحٍ صامت.**

⛔ **وحالة `.coderabbit.yaml` نفسها: مُلتزَمٌ وغير مُثبَتٍ حيّاً بعد.** آخر مراجعة على
الفرع أبلغت حرفياً `Configuration used: defaults` · `Review profile: CHILL` — أي أنّ
الملفّ **لم يُطبَّق**، وهو متوقَّع (الإعداد يُقرأ من فرع الأساس، والملفّ ما زال في هذا
الـPR). فحالته بلغة §6.6 هي **PARTIAL**: `import` ✓ (مُلتزَم ومحروسٌ ببوّابة) ·
`call chain` ✓ (المسار الذي يقرؤه CodeRabbit) · `runtime evidence` ✗. **لا يُرقّى إلى
ACTIVE قبل مراجعةٍ تُبلِّغ إعداداً غير الافتراضي** — والادّعاء بغير ذلك هو نفس الخطأ الذي
يوثّقه هذا الملفّ كلّه.

⚠️ **حدّان بنيويان يبقيان:**
1. CodeRabbit يراجع **الديفّ**، فالأعطاب القديمة في بقيّة المستودع خارج مرماه — نفس عمى
   delta عند CodeScene. Qodana وحده يمسح الشجرة كلّها.
2. **البوّابة تحرس الملفّ لا الحساب.** ظهرت «Pre-merge checks ✅ 5» في المراجعة بلا أن
   يُصرِّح بها الملفّ — فهي إعداداتُ حسابٍ خارج المستودع. `check_ci_workflow_hygiene`
   تمنع أن نجعله حاجزاً **من الملفّ**، ولا تستطيع منع إعدادٍ تنظيمي. يُقال ولا يُكتَم (L11).

## 3.5) الأداة الرابعة والأهمّ: **بوّابات المستودع نفسه**

في هذا العمل بالذات، الأدوات الخارجية الثلاث **مرّت جميعاً** على إصلاحٍ خاطئ، والذي
أوقفه بوّابةٌ كتبها المستودع لنفسه. هذا هو الحدث الذي وُلد منه `.coderabbit.yaml`:

| الحدث | النتيجة |
|------|---------|
| Qodana صنّف `dict[str, any]` **Critical** | ✅ كشفٌ صحيح |
| أُصلحت إلى `dict[str, Any]` | Qodana ✅ · CodeScene ✅ · CodeRabbit ✅ — **الثلاثة مرّوا** |
| `check_no_new_any` (بوّابة محلّية · D-192) | ❌ **رفضت**: «5× `Any` بينما المسموح 0 — صنِّف النوع بدل توسيعه» |
| أُصلحت إلى `dict[str, object]` | الجميع أخضر |

**الدرس:** الأداة الخارجية تعرف **اللغة**، ولا تعرف **قانون مستودعك**. حظرُ `Any` قرارٌ
محلّي (D-192)، ولا محلِّل تجاري يعرفه. ولذلك ⛔ **لا يُستبدَل حارسٌ محلّي بأداةٍ خارجية** —
تُضاف إليه.

### وفي الاتجاه المعاكس: المراجعة أثبتت أنّ **المِسنَن نفسه كان بلا أسنان**

كُتب `fail-threshold: 2405` مفتاحاً مستقلاً تحت `with:` — و`JetBrains/qodana-action@v2026.1`
**لا تُعلن مُدخَلاً بهذا الاسم**، وGitHub يُسقِط المفاتيح غير المُعلَنة **بصمت**. فبقي
الفحص أخضر بينما البوّابة **غير موجودة**، طوال commitين.

| | |
|---|---|
| المُدخَلات المُعلَنة فعلياً | `args` · `pr-mode` · `upload-result` · `use-caches` · `primary-cache-key` · `additional-cache-key` · `artifact-name` · `cache-dir` · `results-dir` · `post-pr-comment` · `use-annotations` · `push-fixes` · `commit-message` · `github-token` · `nightly-version` · `cache-default-branch-only` |
| المكان الصحيح | داخل `args`: `--fail-threshold,2405` |
| مرجع الـCLI | «Set the number of problems that will serve as a quality gate. If this number is **reached**, the inspection run is terminated with a non-zero exit code» |

⛔ **القاعدة:** تُتحقَّق راية الإجراء من **`action.yaml` الخاص به**، لا من README ولا من
ملخّصٍ عنه — وهذا بالضبط مصدر الخطأ هنا.

### البرهان على أنّ للمِسنَن أسناناً الآن — من سجلّ التشغيل لا من الاستنتاج

**١) الراية تصل إلى الـCLI فعلاً** (`1b8cd82` · سطر الأمر من السجلّ نفسه):

```text
[command]…/qodana/2026.1.3/x64/qodana scan --cache-dir … --skip-pull \
  -e QODANA_PYTHON_PATH=/opt/miniconda3/bin/python --fail-threshold 2405
```

**٢) الحدّ مقيس**: نفس التشغيل أبلغ **2405** مشكلة أمام عتبة **2405** وانتهى **success**.
فالإفشال على `count > N` لا `count >= N` — ومرجع الـCLI يقول «إذا **بُلِغ** هذا العدد»،
وهي صياغةٌ أوسع من السلوك. إذن **2405 هو الرقم الصحيح**: يسمح بالشجرة اليوم ويُحمِّر على
مشكلةٍ جديدة واحدة.

⚠️ **وما زال غير مُشاهَد: تشغيلٌ يفشل فعلاً.** تسليمُ الوسيط والحدُّ مقيسان، وفرعُ الفشل
عقدٌ موثَّق من JetBrains لا قياسٌ لدينا. إثباته يكلّف مسحاً أحمر متعمَّداً (عتبة 2404 على
فرعٍ يُرمى) — يُنجَز في أوّل مرّةٍ يُفتَح فيها هذا الملفّ لسببٍ آخر. **بوّابةٌ لم تُشاهَد
وهي تفشل ليست مُثبَتة تماماً**، والفرق بين «مجهولٌ مُعلَن» و«يقينٍ زائف» هو هذه الفقرة.

**٣) وعطبٌ ثالث كشفه السجلّ نفسه** — تنسيق الوسائط بالفواصل **مهجور**:

```text
##[warning]Comma-separated args format is deprecated and will be removed in a future version.
```

و«**سيُزال**» هي الكلمة الخطرة: يومَ يُزال تصير السلسلة كلّها وسيطاً واحداً مشوَّهاً،
وأرجحُ نتيجةٍ أن تتوقّف العتبة عن التطبيق **بصمت** — نفس العطب حرفياً. فحُوِّل إلى الشكل
المدعوم (مسافات) **بينما التحذير ما زال يقول أيّهما هو**.

**تذكيرٌ بصنفٍ لا تجده أيّ أداة ساكنة:** كوارث `.memory/issues.md` الكبرى (ISS-144 ·
ISS-148 · ISS-149) كان **الكشف فيها سليماً والعطب في الأسبقيّة** — ترتيبُ مراحل تُنهي
دور الطالب قبل التي تخدمه. هذه أخطاء **دلالة وترتيب**، تجدها عقود الترانسكريبت وبوّابات
`scripts/fitness/` وحدها.

## 4) الحدود المُعلَنة — لا تُكتَم (L11)

| الحدّ | الأثر |
|------|-------|
| **8 من 13 نتيجة حرجة كاذبة** (مقيسة) | «Critical» يستدعي فحصاً بشرياً لا إصلاحاً آلياً |
| المِسنَن **عددي** لا لكل نتيجة | إصلاحُ مشكلة وإدخالُ أخرى في نفس الـPR **يمرّ** |
| CodeScene يحلّل **`main` وحده** | تغطية فروع الـPR تصل ثمّ **تُهمَل** حتى تُضاف الفروع في إعداداته |
| التغطية `--cov=app` فقط | `microservices/` و`shared/` **غير مقيسة**؛ إن عرضها CodeScene **0٪** بدل «بلا بيانات» فالرقم كاذب ⇒ يُضيَّق النطاق أو يُطفَأ الفحص (D-197) |
| ~~`actions/download-artifact@v6` على Node 20~~ | ✅ **أُصلح** — رُقِّي إلى `@v8` (node24). ⚠️ وكان مكتوباً هنا «لا إصدار أحدث اليوم» وهو **خطأ**: v7 صدر 2025-12 وv8 في 2026-02. ادّعاءُ غيابٍ لم يُفحَص، رصدته المراجعة. |
| رمز Qodana مُعرَّف بمشروع Cloud | إعادة إنشاء المشروع تُبطِل السرّ وتُنتج `403`؛ الـrunbook في خطوة `Explain the failure` |
| **مسح Qodana يستغرق 17–22 دقيقة** (مقيسة: `a814655` 16.7 · `62ce4c2` 19.9 · `42f8876` 22.4) | «4.5 دقيقة» المكتوبة سابقاً كانت زمنَ مسحٍ **بلا مُفسِّر** — سريعاً لأنه خاطئ. المهلة رُفعت 30 → **45** دقيقة: انتهاء المهلة يظهر ❌ **لا يميّزه قارئ عن فشل تحليلٍ حقيقي**، وهو أسوأ صنف إشارة |
| **CodeRabbit يراجع الديفّ وحده** | الأعطاب القديمة خارج مرماه — نفس عمى delta عند CodeScene. Qodana وحده يمسح الشجرة كلّها |
| **CodeRabbit استشاريّ بالبناء** | `fail_commit_status: false` ولا `pre_merge_checks` (D-234) ⇒ **لا يحجب دمجاً أبداً**، والفارض يبقى `scripts/fitness/` |
| ⛔ **CodeRabbit مُقنَّن الاستعمال، ويتخطّى commitات بصمتٍ نسبيّ** | «Review limit reached … next review available in 22 minutes» على `23e0800` و`cf81e01` — أي أنّ **دفعتين لم تُراجَعا**، ويبقى ملخّصُ commit سابق معروضاً فيبدو أنّ المراجعة جرت. ⇒ **الأخضر عنده ليس دليل مراجعة**، والاستئناف بتعليق `@coderabbitai review` |
| **الثلاثة تصل بعد الدفع** | لا واحدة منها فحصٌ قبل الـcommit. الحلقة القصيرة هي `scripts/fitness/` + ruff محلّياً (D-233) |
| ⛔ **الجداول الجزئية** (§2) | صفوف الشدّة تُغطّي 293/320 و1,886/2,078 — الباقي فئاتٌ صغيرة **غير مُعدَّدة**، لا صفر |

## 5) المفتوح

| البند | الحالة |
|------|--------|
| ⛔ **`frontend/` غير ممسوح بـQodana إطلاقاً** | `qodana.yaml` يعلن `linter: qodana-python`، فكلّ الـJS/JSX/TS خارج المسح: **33 ملفّاً · 7,844 سطراً**، أي **≈3.5٪** من الشجرة (مقابل 1,417 ملفّاً و217,994 سطر بايثون). فالرقم 2,405 يصف **البايثون وحده** ولا يصف المستودع. الترقية: `qodana-js` كوظيفةٍ ثانية **بمِسنَنٍ مستقلّ** — ⛔ ولا يُجمَع العدّان في رقمٍ واحد |
| **`lint-frontend` كان لا يستطيع الفشل** | ⚠️ **مُصلَح جزئياً.** كان `npx eslint … --max-warnings 0 \|\| true` — و`\|\| true` تبتلع المخرَج فيصير `--max-warnings 0` زينةً، ووظيفةٌ اسمها «lint» تُبلِّغ أخضر مهما قال ESLint. أُعيد إنتاجه محلّياً بالأمر نفسه: **exit 1** · `✖ 3 problems (0 errors, 3 warnings)` (`react-hooks/exhaustive-deps` في `CogniForgeApp.jsx` ×2 و`CombinationsVisualizer.jsx` ×1). صار مِسنَناً على **العدد المقيس 3** (يتقلّص فقط)، فالرابعة تُحمِّر اليوم. ⛔ **والثلاث لم تُصلَح**: إصلاحها يغيّر مصفوفات تبعيات الـhooks في المكوّن الرئيسي للطالب — تغييرٌ سلوكي بـPR خاصّ به |
| 28 «استدعاء غير قابل للاستدعاء» · 6 «كود غير قابل للوصول» | **غير مفحوصة** — داخل المِسنَن، تُفحَص عند الحاجة |
| 106 مهجور · 95 هروب regex · 44 استيراد غير مستعمل | تنظيف آلي مؤجَّل |
| 2,078 نتيجة متوسطة | أسلوبية — لا خطّة |
| الإنذارات الثمانية الكاذبة | ⛔ **لا تُسكَت** بـ`suppress` ولا يُضاف `: TypeAlias` لإرضاء الأداة — الكود صحيح، وتغييره لإسكات محلِّلٍ مخطئ يخدم الرقم لا القارئ |
